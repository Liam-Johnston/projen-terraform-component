# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

version: "3.3"
services:
  app:
    build:
      context: ./containers
      dockerfile: Dockerfile.app
      args:
        BUN_VERSION: 1.0.14-alpine
    environment:
      NPM_TOKEN: ${NPM_TOKEN:-}
    volumes:
      - type: bind
        source: ./
        target: /app
